{
  "name": "todo-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:unified": "cd frontend && npm run build && cd .. && rmdir /s /q backend\\public && xcopy /E /I /Y frontend\\dist backend\\public && cd backend && npm run build"
  }
}